const initState = {
    id: '',
    displayName: '',
    avatar:'',
    isLogin: false
}

export default (state = initState, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state
    }
}